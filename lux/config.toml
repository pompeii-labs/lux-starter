project_id = ""
project_name = "lux-lab"

# Validation builds use a deliberately non-release image tag. Build the current
# engine stack with `docker build -t ghcr.io/lux-db/lux:pr-validation ../lux`.
engine_version = "pr-validation"

# Stable ports keep the tunnel and device configuration deterministic.
local_http_port = 15890
local_resp_port = 15894
