# Copy to .env — `lux start` prints these values for local dev
PUBLIC_LUX_URL=http://localhost:8080
PUBLIC_LUX_PUBLISHABLE_KEY=lux_pub_xxx

# Base URL of the Hono API (apps/api)
PUBLIC_API_URL=http://localhost:3000
